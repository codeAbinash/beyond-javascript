function hello(name) {
    console.log("Hello " + name)
}

hello("Abinash") // "Hello Abinash"

// "Abinash" is passed for the `name` parameter in the `hello` function.
// this value can be accessed as `name` in the function