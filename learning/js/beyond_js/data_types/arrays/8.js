const arr = ['Is', 'this', 'an', 'array', '🤔'];

console.log(typeof arr); // 'object'
console.log(Array.isArray(arr)); // true, it is an array
console.log(arr instanceof Array); // true, is an array
