const values = [1, 4, "hi", -88, false]

console.log(values[0]) // 1, The first element 
console.log(values[1]) // 4, The second element 

values[0] = "Abinash"
values[1]++ // Increment the value of the second element by 1

console.log(values)