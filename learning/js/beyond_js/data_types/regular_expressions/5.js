let str = 'I have a pen. The pen is very nice.'
let exp = /pen/g // checks if starts with 'Hello'

console.log(exp.test(str)) // true
console.log(exp.test(str)) // true
console.log(exp.test(str)) // false