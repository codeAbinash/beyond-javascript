const person = {
  name: 'Tukai',
};

person.name = 'Abinash';
person.age = 20;
person['language'] = 'JavaScript';
// Assigns Object Property

console.log(person.name); // "Abinash"
console.log(person.age); // 20
console.log(person.language); // "JavaScript"
