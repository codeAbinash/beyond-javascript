{
    let p = 1
    let q = 2
    let r = 3
    console.log(p, q, r)
}
// Grouping 4 Statements using {}

if (true) { // Expects single statement
    let x = "Hey"
    let y = "Hello World"
    console.log(x, y)
    // We are grouping 3 statements into one
}else{

} // Empty Block, useful when we have to write code later in this block